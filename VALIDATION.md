# Validation

The follow-up defect audit fixes four test blind spots: shell echo mistaken for Pi survival after Ctrl+C, silent goal callbacks masking leaked timers, portable compaction missing edited input, and transport expectations aliasing a mutable request. The same isolated faults pass the old checks and fail the corrected checks. Current Node 22 tests pass **320/320**, zero failures/skips, and test types pass (`.work/followup-all-tests.log`, `.work/followup-test-types.log`). Current strict **E1–E6** passes with exactly 161 clipboard characters (`.work/followup-pty-e4-normal.log`). Full maintenance is **7,991 physical lines**: 7,908 test/support/PTY + 42 runner + 4 package + 3 CI + 34 production net increase. The audit changes four test/PTY files, net -1 line / +149 bytes; staged whitespace validation then removes eight redundant EOF blank lines (no executable changes); production, vendor and support are unchanged. Prior full verify types/freshness/1,250-file package evidence remains `.work/main-tests-final-verify.log`; no build was repeated during PTY. Scope, failed first probe and before/after evidence: `.work/metis-pi-followup-fault-audit.md`. No commit/push or remote CI execution is claimed.

## Historical verification evidence

The results below describe earlier stages. Their counts and ownership notes are historical; the current evidence and corrected full accounting are above.
## Earlier third algorithm batch

The preceding third algorithm batch passed complete `npm run verify` on Node 22.19.0 / npm 10.9.3: **332/332 tests**, zero failures/skips, project/test/vendor types, freshness and a 1,250-file package dry-run (`.work/test-algorithms-round3-verify.log`). Vendor regeneration leaves no diff. Twenty-one test files remove 276 lines / 11,723 bytes; full maintenance is **8,125 lines**, still 125 above the hard ceiling. Real Linux execution and heap checks pass; black-theme, HEAD fallback, timer, late-result and namespace-alias faults remain detected. Production, support and PTY code did not change in this batch. The latest strict E1–E6 evidence remains `.work/algorithm2-pty-strict.log`, including exact 161-character copy to the isolated sink. Remote CI has not been observed. The goal remains active.

## Earlier second algorithm batch

The preceding second algorithm batch passed complete `npm run verify` on Node 22.19.0 / npm 10.9.3: 352/352 tests, zero failures/skips, all project/test/vendor type checks, vendor freshness and a 1,250-file package dry-run (`.work/test-algorithms-round2-verify.log`). Vendor regeneration leaves no diff. Strict E1–E6 also passes with 161 exact clipboard characters in the isolated sink (`.work/algorithm2-pty-strict.log`); the build ran after PTY finished. Eighteen tests and the PTY driver remove another 401 lines / 19,704 bytes. Full maintenance is 8,401 lines, 401 above the hard ceiling; the goal remains active. Both PTY commands now require all selected journeys: missing Pi/tmux, Git for E1, or `PCX_PTY_SKIP_WHEEL=1` fails. Four default-mode failure probes confirm this behavior. No production or support code changed.

## Earlier algorithm batch

The preceding algorithm overhaul passed the complete `npm run verify` on Node 22.19.0 / npm 10.9.3: 378/378 tests, zero failures/skips, project/test/vendor type checks, vendor freshness and a 1,250-file package dry-run (`.work/test-algorithms-verify.log`). Vendor regeneration leaves no diff. Nineteen test files now use simpler boundary examples and shared lifecycles, removing 411 lines / 19,164 bytes. At that stage full maintenance was 8,802 lines, still 802 above the hard ceiling; the new goal remains active. Five cross-layer fault probes, four protocol probes, three chrome probes and the command line-break probe remain discriminating. Production, support and PTY code did not change in this batch; the existing strict E1–E6 evidence remains `.work/terminal-layout-pty-strict.log`.

## Prior evidence

The preceding test-only consolidation passed 398/398 Node tests with zero failures/skips and test type checking (`.work/contract-reachability-{all,types}.log`). Appearance now owns persisted summary schema, timestamps, duplicate completion and registered rendering through its event chain; the separate direct-record fixture was removed after checking production reachability. Five existing fault probes still fail at their intended owners; removing the settlement gate or record call also fails the host contract. Todo's unused transition combinations and exhaustive permutation framework were retired; direct hierarchy expectations and actual tool-level rotation, reopen and skip rejection remain. The final reopen assertion was then strengthened to check that the old completion turn is cleared: the affected tool suite passes 9/9, and suppressing that reset fails the intended assertion (`.work/todo-reopen-turn{,-mutation}.log`). The combined reduction is 113 lines, with no production or PTY changes.

The preceding complete `npm run verify` passed: 417/417 tests, zero failures/skips, project/test/vendor type checks, vendor freshness and a 1,250-file package dry-run (`.work/main-tests-requirements-audit-verify.log`). Vendor regeneration leaves no diff. The latest strict E1–E6 run, including exact 161-character clipboard text, remains `.work/terminal-layout-pty-strict.log`; subsequent changes affect test fixtures and documentation only. At that stage full maintenance was 9,213 lines, so the 8,000-line goal is not complete.

### Recent incremental evidence

The latest fixture ownership change passes 18/18 focused tests: host startup registers cleanup before installing the session, global warning mocks restore even if fixture construction fails, and four temporary directories reuse the existing lease. Loader-only failure probes confirm restoration after partial startup and distinguish the old console leak from the corrected behavior. No production or PTY changes were made. After the preceding terminal geometry fixture change, renderer tests pass 11/11 and strict PTY E1–E6 passes, including exact 161-character clipboard text (`.work/terminal-layout-renderers.log`, `.work/terminal-layout-pty-strict.log`). Pi now owns width/wrap calculations; independent probes catch the old combining-character and ZWJ mouse-column errors. The preceding full Node suite passes 417/417 with zero failures or skips, and test type checking passes (`.work/main-tests-value-followup-{node,types}.log`). This latest batch changes tests only. The preceding complete `npm run verify` passes locally on Node 22.19.0 / npm 10.9.3: 422/422 tests, zero failures or skips, across 68 test files, plus project/test/vendor type checks, vendor freshness and package dry-run (1,250 files). It includes the architecture refactors, independent expectations and latest consolidation of complete copy and Git journeys. Log: `.work/main-tests-journey-owner-verify.log`. Current strict E1–E6 passes after the PTY boundary consolidation, including exact 161-character Ctrl+C copy and real todo restart (`.work/terminal-layout-pty-strict.log`). No production code changed in this batch. Remote CI has not been observed. Earlier evidence remains below; commands are documented in [development.md](docs/development.md).

## Historical checks

Git tracker consolidation passes 25/25 focused tests and is included in the latest full run. Timer and stale-read mutations still fail. A forced HEAD-error-to-unborn fallback passed 6/6 with the old conflated command fixture; separating HEAD and object-format responses makes both affected contracts fail.

The prior risk-based cleanup removed 347 lines and 14 tests: animation implementation details, duplicate registration and todo flows, lexer/serializer self-comparisons, arbitrary malformed inputs and a normalized random-input framework. Independent wire expectations, exact copy provenance, real host wiring and resource regressions remain. The latest batch removes another 102 lines: E3 uses one direct gesture chain; E6 checks both menu triggers in one draft and submits two skill bodies once. Waiting for the sole intended completion fixes a stale-menu race exposed by the consolidation. Exec/Git/write cleanup removes internal layout/count constraints and unreachable fixtures; Git interval and late-read mutations still fail. The latest complete-flow consolidation removes another 93 net lines: PTY E1 owns live Git footer updates and colors; E4 owns exact copy below clipped history, draft preservation and clear. Three duplicate contract flows were removed. The isolated Pi process explicitly enables truecolor; a corrupted clipboard payload fails the exact-text check. A further value review removes 124 lines: duplicate simulated skill installation, a self-implemented completion fixture, native tree and diagnostic constraints, private factory/timestamp assertions, and a bash entry smoke now owned by PTY E2. The image projection migration was rejected because complete native rows and call projections are different observation boundaries. Removing the duplicate geometry implementations saves another 4 lines. The cleanup ownership changes remove another 6 lines. At that stage full maintenance was 9,213 lines, above the required 8,000 ceiling; the overhaul is not complete.

| Check | Result |
| --- | --- |
| Earlier full Node tests | At that stage full verify ran 450/450 on Node 22.19.0 / npm 10.9.3, zero skips; includes every current migration and both chrome and todo session cleanup repairs. |
| Earlier full `npm run verify` | Exit 0 on Node 22.19.0 / npm 10.9.3: 450/450 tests, zero skips; project/test/vendor types, vendor freshness and pack dry-run (1,250 files). Vendor rebuild leaves dist unchanged. This is local evidence, not a remote CI result. |
| Rule/host separation and readiness | Chrome and transcript now have separate rule suites, appearance session contracts, native editor contracts and native thinking contracts. All migrated cases remain in the default runner; 37 targeted tests pass. Session installation exposes its completion Promise, covering disabled/failed/cancelled installs and superseded generations without a Working callback latch or fixed delay. |
| Git execution boundaries | Parser and injected HEAD decisions run without resource fixtures; command/tracker tests control processes and clocks; IO tests retain actual Git and filesystem behavior. Appearance reuses the same minimal repository constructor. All 36 targeted tests and the 10-test IO entry pass; real and injected HEAD cases now report independently. |
| Todo dependency boundaries | Pure rows use explicit snapshots; widgets depend on snapshot/settings/turn. The entry owns disk/settings integration, replacement UI installation, shutdown and restart. Its new ownership assertion fails before the repair; after it, todo contract/IO tests pass 23/23, full verify passes 450/450 and strict PTY E5 passes. |
| Pure execution group | `test:fast` selects only core files: 184/184 pass. Skill input/completion has no filesystem/host runtime dependency; real discovery/parser stays in IO. Diff/LCS/budget rules are separate from write lifecycle IO. Full Node suite after these moves: 489/489. Strict PTY E6 passes after the production skill extraction. |
| Native adapter and copy ownership | All 16 adapter cases use actual Pi rows with independent prototype leases; no test implementation of the host render tree remains. Copy semantic cases have per-test leases, resource cases own cache/heap budgets, and editor owns native hardware-cursor wiring. Chrome group 94/94 and full suite 489/489 pass. |
| Execution layers and vendor boundary | All 69 test files have one primary execution group (core/contract/protocol/IO/resource plus package checks). Protocol cases register only the built provider; the full-entry contract verifies schema/catalog inheritance and one final prompt/input capture. Network guards are explicit per-test leases. All 489 tests, 183 fast tests, the 2-test vendor entry and test type checks pass. |
| Lifecycle consolidation | Appearance uses one interaction for preview/final usage, duplicate completion, two response clocks, Working and persisted summary; goal uses one clock/account/pause/resume/complete lifecycle. Full suite passes 484/484. A runtime-only mutation suppressing the second response reset fails at the intended checkpoint (200 versus 100 tok/s); normal code passes. Goal fixtures always shut down and own their mock clock. |
| Interaction and thinking ownership | Named outcome/time profiles retain explicit verdicts and full speed samples. UiMetrics tests own mock intervals, run the scheduled callbacks and reset after each test. Global show shares the main thinking policy trajectory; history still constructs the actual native assistant without replayed events. The duplicate decoration harness and its theme dependency are gone. Full suite 484/484, core 184/184 and test type checks pass. |
| Todo projection and fixture ownership | All 120 storage permutations check independently written path, depth and order expectations. A loader-only mutation sorting both projections by ID fails the new oracle. Finished-list rules use a legal snapshot, and plain-text list rendering runs without a store; tool/IO boundaries remain covered. Focused todo tests 49/49, full suite 483/483, core 184/184 and test type checks pass. |
| Compaction session ownership | Replay, request and context-edit suites share real in-memory Pi sessions. Handwritten entry IDs, parent chains and duplicate checkpoint construction are removed; historical stored snapshots remain explicit compatibility inputs. Independent wire expectations, all seven boundary forms and native/portable checks remain. Protocol 62/62, full Node suite 483/483 and test type checks pass with zero skips. |
| Transcript lifecycle ownership | Native thinking collapse, visible-only copy, manual expansion and global show share one component lifecycle; separator replay includes the next-message boundary, and two-run rendering checks rail/text ownership. Serial reads check membership, totals, refresh hints and image notices after every append, using independent tail positions. All 479 Node tests, test type checks and 14 focused Node 22 tests pass. A runtime-only mutation marking every member as non-tail fails the required image-notice assertion. |
| Git worktree lifecycle | One real repository carries existing WIP, mixed staged/unstaged changes, a staged new file, rewrite/revert, a path-limited partial commit and final cleanup, with explicit counts at every boundary. Initial text/binary fixtures are committed directly. Unborn-to-first-commit owns real HEAD resolution; damaged HEAD, rename, IO filtering, timers and races remain independent. All 30 affected tests, full suite 476/476 and test type checks pass. |
| Todo entry capabilities and IO ownership | The entry fixture records the actual registered tool, commands, events and widgets against one context, without a parallel FakeHost state object. Prompt and steer share a lifecycle; normal activation reports no warning. The real IO bridge now invokes the entry-installed widget and `/todos` handler, verifies external task edits and persisted hide/show settings through a reopened store. Widget tests use controlled snapshots only. Focused 15/15, full Node suite 476/476 and test type checks pass. |
| Copy/editor and prototype ownership | Ctrl+C selection/draft/native fallback is owned by the editor suite; cache throttling uses a real Text subclass. The repeated-install test failed when selected alone because it relied on a preceding entry test. It now establishes its own installation and sibling-session lifecycle, and passes alone; Ctrl+C also passes alone. Focused 23/23, full Node 476/476 and test type checks pass. |
| Compaction lifecycle ownership | One suite carries each real session through serializer, checkpoint replay and final compaction requests; transcript owns schema/pairing/defer-loading goldens. Context-edit boundary coverage stays separate. Focused 13/13, protocol 59/59, full Node 473/473 and Node 22 full verify pass. Loader-only mutations dropping the final trigger or replayed sealed window each fail the corresponding boundary assertion. |
| Activation ownership | Appearance uses an isolated real Pi subclass for activation, disabled/noninteractive paths, repeated sessions and declared API limits, with descriptor restoration checked at teardown. Adapter now owns only adapter rules and native row contracts. Both share a 16-line subclass factory. Focused 25/25, full Node 472/472 and test type checks pass; bypassing the global enabled gate fails the native-renderer identity assertion in the combined disabled test. |
| Skill gesture ownership | Removed the FakeBox/FakeSkillEntry state machine. Core owns forwarding, idempotence and absent capabilities; native contracts use independent Pi subclasses for nested labels, programmatic/gesture toggles, one invalidation per click and modified-press selection. Focused 7/7, full Node 469/469 and test type checks pass. A loader-only mutation removing invalidate fails the native cache-invalidation assertion (0 versus 1). |
| Background widget ownership | All cases now use Pi's real widget registration and above-editor container, with the registered component as the sole component state. One lifecycle checks direct and routed clicks, shortcut parity, render requests, ignored gestures, refresh, session selection/termination and removal. Empty/non-TUI cases remain independent. Focused 2/2 passes; production and PTY are unchanged. |
| Interaction clock lifecycle | Retry/gap timing, two thinking intervals, duplicate usage, settlement and next-prompt reset share one trajectory with intermediate literal expectations. Ticker and writing-phase boundaries remain independent. Related core tests pass 37/37 and test types pass. Runtime mutations resetting the retry clock (2,000 versus 8,000 ms) and ignoring thinkingEnd (4,500 versus 4,000 ms) fail their distinct checkpoints. |
| Tool-row entry ownership | Real builtin read definitions, grouped titles, unchanged stock updates, native image identity/content/height, foreign same-name self-shell and shutdown tree restoration share the shipped entry. Dynamic enabled gating remains in the adapter suite. Full Node 463/463 and test type checks pass; deleting entry tool-start forwarding fails the grouped-title assertion. |
| Native clipboard host boundary | Facade tests use isolated subclasses of real TuiAltScreen, preserving native copy/feedback behavior and scoped prototype leases. Ctrl+C invokes the native method with only the backend replaced. Focused 34/34 and test type checks pass; dropping native feedback causes all three facade outcome checks to fail. |
| Git scheduled/in-flight ownership | One controlled command fixture owns immediate and suspended reads. Timer checks observe scheduled work without manually refreshing, and disposal waits until diff is actually in flight. Both interval-removal and stale-result mutations passed the former tests and fail the revised independent snapshot assertions. Git 20/20 and test type checks pass. |
| Shell display/copy ownership | Layout uses independent literal row budgets. Copy owns a genuinely truncated suffix, hard/soft boundaries and exact text, without importing display limits. Enlarging either production budget passes the former tests and fails the revised layout suite. Focused 26/26 and Node 22 full verify pass; final copy assertion passes 6/6 on Node 22. |
| Todo tool/storage capabilities | Tool mapping uses only read/mutate with legal data seeds and real model functions; it no longer opens a disk store. Evidence-file validation belongs to IO and checks a reopened persisted completion. Command diagnostics independently require read/status/collect. Focused 25/25, full Node 464/464 and project/test type checks pass. |
| Footer metadata lifecycle | One activated footer carries ordered unique fields, vendor status, 64-frame projection reuse, same-revision model replacement, ten refresh events, late leaf append, unknown usage and session replacement. Hidden metadata and installation/Working/Git boundaries stay separate. Focused 19/19 and test type checks pass. |
| Native thinking wrapper ownership | Actual rail/peek/clickable components replace handwritten wrappers and direct controller calls. MouseRegion gestures reveal a genuinely clipped body, completion folds once, and later gestures survive rebuilds. Existing multi-run/global-show/copy/history cases remain. Focused 34/34 and test type checks pass; swallowing clicks before controller forwarding fails the full-body assertion. |
| Tool component dependency boundary | Shell/diff factories and copy caches live in src with injected LayoutOps; no direct host imports. Shell contracts use isolated native Pi rows and real adapter/copy components without full activation. All original cache/stream/expand/theme/scroll/copy/image scenarios remain: 17 focused checks, four forbidden-entry checks and full Node 22 verify pass. |
| Pending-window lifecycle ownership | A real compaction handler produces the window used to check prewarm under both native settings, ordinary-request preservation, final-summary consumption, prompt capture/auth counts and cross-session rejection. Preparation retains its independent mode/order/failure matrix and no longer constructs checkpoints. Context runtime uses explicit capabilities. Focused 38/38 and Node 22 protocol 57/57 pass; premature-prewarm-consumption and missing-final-clear mutations both fail the lifecycle owner. |
| Native write lifecycle | Four partial-argument frames, real Pi write execution, exact persisted content, entry events and completed folded/expanded display share one native row. A fabricated write result and a separate streaming-only setup were removed. All padded edit and real apply_patch/Jiti witnesses remain; focused entry 6/6 and Node 22 family 25/25 pass. |
| History/fullscreen dependency ownership | History directly mounts the existing system on a real TUI/ScrollView, without installing fullscreen prototype hooks. Only the copy case installs required copy wrappers. Fullscreen retains integration, native restore, foreign-wrapper preservation and reacquisition in one explicit trajectory. Focused 20/20 and forbidden-fullscreen history 14/14 pass; unconditional setter restoration fails at foreign-owner preservation. |
| Vendor entry and provider ownership | Catalog/refresh/Reserve/idempotence use the public provider registration API. The built entry loads through real DefaultResourceLoader/AgentSession, with actual startup/shutdown, native provider lookup, tool schemas and final payload capture. No handwritten Pi API/context or handler-name lookup remains. Node 22 host group 116/116; renaming the private callback still passes 2/2, while disconnecting session_start fails the entry native-provider assertion. Production and PTY unchanged. |
| Current `npm run test:pty:strict` | Exit 0 after terminal boundary consolidation: E1–E6, one wheel-up event visibly changes the peek window, real Ctrl+C delivers exactly 161 characters, and the draft reaches the provider. E3 retains single-click and double-click rail routing. E6 reopens a dead menu with second-token `/` and `￥` in one draft, waits for the correct sole candidate, and verifies both skill bodies in one request plus one label click. E2 still checks real bash stdout; E5 restarts with its real completed store. |
| Todo width and global disable | Todo rows use Pi's terminal-column truncation: a 4-column Chinese counterexample failed before the fix, then CJK, combining and ZWJ emoji passed at widths 1–40. With `enabled=false`, a valid host tool prototype remains native; that case also failed before the fix. Both repairs passed `verify` and the six strict PTY journeys. |
| Layout and transcript ownership | Real TUI tests reproduce the old repeated-capture teardown failure, then verify single-hook retries, native descriptor/root/wheel/input restoration and later-wrapper survival through reacquisition. Gutter/history composition uses an actual Container, so history mounts. Real grouped tools retain their output, images, third-party renderers and stock fallback without the obsolete `updateDisplay` spacer mutation. Todo tests include all 120 creation orders of a five-node hierarchy, reparented tool/widget indentation, orphan labels and explicit parent completion. |
| Copy and string allocation | Direct-text spans match the saved baseline for 1,008 wrap cases, 130 Text/Markdown renders and 8,522 exact selections. Whole native self-shell tests cover gutters, soft joins, warm cache, committed old frames and image fallback. Three 3,600-row/223,289-character scenes retain ~3.03 MiB before versus ~2.98 MiB after; a 6 MiB subprocess gate catches per-grapheme string-rope growth. ANSI stripping matches 10,000 control/malformed-sequence cases against 312bc5d; an isolated 2,000-call benchmark measured ~124→1 ms for plain 8 Ki-character input and ~218→86 ms for colored 12.8k-character input. These are helper measurements, not overall TUI speed claims. |
| Context contract and request preparation | Independent pre-refactor goldens cover all nine flat/namespace operations, required/nullable fields, Remote omissions, encryption and per-request schema ownership. Runtime tests cover invalid/empty arguments, remote failures and stream routing. Live/prewarm tests cover Local/Tree/Remote/Responses Lite, mismatched provider/API predicates, ordering, final-only effects, pending-window preservation and preparation failure. No real backend was contacted. |
| Memory ownership and lifecycle | `io/exec-output` owns storage/recovery without a native-manager dependency; `resource/exec-session` owns native exit delivery/replay, unread background exit, execution/poll cancellation, wait failure, TTY continuation and distinct read/disposal/shutdown failures; `resource/exec-output-memory` owns the isolated heap budget. Only the spool-directory observer is shared. All 17 cases and the full Node 22 suite pass; forbidding manager/bridge imports in both parent and child processes still passes all 7 storage/heap cases. A subprocess retains 64 Mi characters with <8 MiB added JS heap. Earlier isolated comparisons measured ~64.05 MiB for the string accumulator versus ~0.119 MiB for the disk ring; this is retained JS heap, not total system RAM. |
| Diff and request allocation | `test/core/write-diff.test.mjs`、`test/io/write-tracker.test.mjs` covers exact 4k-line rewrites, the 4 MiB search-array budget, fail-closed behavior and independently counted minimal edits for repeated-line examples; backtracking now directly numbers rows without a second operation representation. The same 2k-line full rewrite measured ~100 ms/122 MiB trace arrays before versus ~1.9 ms/no trace arrays after. Canonical tests forbid cloning during validation and cover implicit/shared/distinct reconstructed inputs, caller mutation and independent response/replay ownership. Built-transport tests cover successful WebSocket (zero SSE compression), SSE and fallback with actual compressed payload decoding. |
| Chrome session-generation ownership | Readiness and footer capability loss/failure/restoration now reuse one activation; static config gates use independent activations because config loads once. This exposed old footerInstalled state restarting a poller after the footer became unavailable. session_start restores the previous UI before rebinding. Old tests pass 9/9 without the fix; new tests catch missing restoration and restoration on the wrong UI. Current full verify and strict PTY pass. |
| Todo persistence lifecycle | One real directory now owns initial batch persistence, claims, 1- and 8-write queues with literal totals, disposal/reopen with exact disk comparison, and corrupt-file archiving. GC, snapshot caching, stat/read errors and read/fingerprint races remain separate. Related store/widget/entry 15/15 and check:test pass; production unchanged, test-only net −8 lines. |
| UsageLedger state ownership | Live confirmation, duplicate replay, final correction, weighted totals, caller snapshot isolation and session rebuild share one ledger. Custom entries remain excluded and compaction usage included. The reconstruction now proves previous request keys are discarded. Related 23/23 and check:test pass; removing rebuild reset passes the old 5/5 but fails the new total at 6150 versus 150. Test-only net −7 lines. |
| HostData snapshot ownership | The core owner now binds only model/session/getContextUsage capabilities. One lifecycle covers the receiver, live model replacement, in-place window updates, revision/leaf/session invalidation and unbind/rebind; independent field validation and unknown/error/retry remain. Removed the broad host-shaped fixture and redundant standalone setup. Focused core/appearance 13/13 and check:test pass; removing the getter receiver fails the explicit snapshot checkpoint. Test-only net −37 lines. |
| Cached rendering and cleanup | Goal lifecycle tests release old timers/context on shutdown and restore. HostData/chrome tests cover message/model/leaf/session/compaction invalidation, transient context errors, hidden metadata and hidden/failed/late footer installs. Todo tests cover same-size edits, atomic replacement, deletion/recreation, stat/read races and explicit unavailable→recovered display. An unchanged 10k-message session measured ~1.04 ms per native context read versus ~0.000324 ms per cached read, with invalidation verified. |
| exec_command layout and colors | `npm test`: 367/367; `check`, `check:core`, `vendor:check`, `test:host` and `vendor:smoke`: exit 0. Vendor dist and patch regenerated. Real host tests compare single-line chains and indented/CJK heredocs to the builtin bash layout at 20/40/80/160 columns through fold/expand/refold, with aligned copy rows and unchanged results. A packed copy without local Pi peers passed Pi 0.87.1 fresh-loader verification: actual execution, token colors, width-aware long chains, original shell and teardown. Existing installed package was not modified. Full PTY and pristine vendor replay were not rerun. |
| apply_patch display regression | `npm test`: 363/363; `npm run check`, `vendor:check` and `test:host`: exit 0. Vendor build and local patch regenerated. Installed Pi 0.87.1 fresh-loader probe against the development checkout passed in folded, expanded and re-folded states. A subsequent normal Git installation exposed module-local state isolation; separate Jiti-context regressions now cover shared snapshot, failure state, compact policy and shutdown. PTY and pristine patch replay were not rerun for this change. |
| Historical `env -u NO_COLOR npm run verify` | Exit 0: 457/457 tests, project/vendor type checks, vendor activation, real Pi 0.87.0 host smoke and package dry-run. Includes native completion failure injection: failed reads retain the error-owning session; failed disposal removes the session but cannot publish a successful replay. |
| `npm run check:core` | Exit 0. Pure rendering/state code retains the host-effect boundary. |
| `npm run preview` | Exit 0. Message separators resolve through object identity; plaintext matches the committed preview. ANSI/HTML were regenerated. |
| Historical `env -u NO_COLOR npm run test:pty` | An earlier harness failed at `wheelUntil` on Pi 0.87.0 and 0.86.1. The current one-action wheel witness passes locally. |
| Historical `PCX_PTY_SKIP_WHEEL=1 env -u NO_COLOR npm run test:pty` | Skipped the wheel checks and used reverse-video capture and copy telemetry. The current strict stage also compares the actual clipboard method payload with an independent expected string. |
| Vendor patch delivery | Pristine 3.0.34 replay matches all 424 retained source/payload files byte-for-byte and in permissions. Sandbox CLI tests reproduce the old partial-stdout publication bug and verify missing git, failures, signals and buffer overflow preserve the last artifact, while real diff exits 0/1 publish correctly. |
| Repeated `vendor:build` / `vendor:patch` | Identical dist digest and byte-identical patch. |
| `/btw` seeding patch | Offline harness against real 0.87.0 and 0.86.1 `createAgentSession`: baseline fails on 0.87.0, patched passes on both. No auth, network or user session involved. |

## Behavior protected

| Area | Evidence |
| --- | --- |
| Real composer caret | The first `▏` patch hid the character under the cursor (screenshot `hello wor▏d`); removed that substitution. Real `CustomEditor` keeps `hello world`, the IME marker and cell width unchanged, while real TUI emits DECSCUSR steady bar and shows the positioned hardware cursor. Verified with local Pi 0.87.0 and global Pi 0.87.1 in both main-screen and fullscreen rendering; hardware-cursor acquisition, renderer changes, fallback and release restore are tested. `npm test`: 369/369; root/vendor checks, vendor activation and host smoke pass. No installed-package update or physical-terminal cursor screenshot was performed. |
| Footer metadata relocation | `test/core/chrome.test.mjs` and `test/contract/appearance.test.mjs` verify model → effort → provider → path → context → I/O → cache, live model switching, no metadata widget, unknown vs zero context, and ordered narrow-width wrapping (60–200 cells). `npm test`: 365/365; root/vendor type checks, vendor activation and real-host smoke pass. The real TUI PTY confirmed placement below the editor, then timed out on its later git-change rewrite expectation (`+2 -3`, observed `+2 -0`), twice; its full run is not claimed as passing. |
| Footer quota removal | `test/contract/appearance.test.mjs` checks the provider-independent status contract with a Codex model: right-side quota is absent while the vendor-owned `Codex adapter` status line remains. Obsolete config keys are ignored. At removal: 365/365 tests; root/vendor type checks, vendor activation and real-host smoke passed. The old app-server reader and refresh timer were removed. |
| Background shell mouse toggle | `test/contract/vendor-background-bash-widget.test.mjs`: four cases cover click/refold, shared shortcut state, refresh/selection/close, pass-through gestures, narrow widths, empty/headless cases and real Pi widget container routing. At the time: 371/371; root/vendor type checks, vendor activation and host smoke pass. A working-tree component also passed click/refold through installed Pi 0.87.1's above-editor host container; no physical-terminal click or installed-package update was performed. |
| Pi 0.87 context edits | `test/protocol/vendor-codex-context-edits.test.mjs` builds real 0.87 `SessionManager` sessions (`appendMessage`/`appendContextEdit`/`appendCompaction`) and compares reconstruction and replay with the payload Pi builds from the same session. Thirty-nine cases cover deletion, replacement, repeated edits on one target, absorbed edits that predate the checkpoint, same-target edits on both sides of it, a rebuild that recovers through a new checkpoint (including a third checkpoint), retain-none windows (0.87's checkpoint-id form and the legacy 0.86 null form, which the 0.86.1 host case also exercises), tool-call pairing, untouched prefixes, system sections, the canonical baseline, and a parameterized handler block that runs seven boundary shapes (missing field, explicit `undefined`, unknown id, id after the checkpoint, legacy `null`, the checkpoint's own id, a kept entry) through the production `handleCodexSessionBeforeCompact` with the portable summary both off and on, asserting native and portable request counts for each. Five of the missing/undefined and portable-request cases fail against the round-3 vendor build. |
| Outbound window gating | `buildNativeCompactionInput` returns `{ok:false,reason}` for a checkpoint whose `firstKeptEntryId` is missing, explicitly `undefined`, or does not resolve on the active path, and the handler cancels before any summary request, so an unresolvable boundary can never put the old opaque window on the wire and the optional portable summary is not attempted either. `resolveOpaqueNativeCompactionFallbackEntry` and `injectNativeWindowIntoPiCompactionRequest` (returning `injected` / `not-applicable` / `rejected`) share `inspectCheckpointWindow`, which also keeps a later kept-window edit out of replay, the portable summary and the Pi fallback. The portable summary keeps its window locally instead of parking it in shared state, and a window that an asynchronous consumer re-reads is re-validated against its source checkpoint at that boundary. |
| Retain-none checkpoints | The same file generates `appendCompaction(..., null, ...)` through the real session manager, asserts the empty kept window plus live tail replayed in strict mode, and keeps `first-kept-entry-not-found` for unknown ids and normal kept windows for old sessions. Only the two host markers are retain-none: the checkpoint's own id (0.87) and a present `null` (the 0.86 stored form). A missing field, an explicit `undefined` or an id after the checkpoint is rejected instead of degrading into an empty kept window. |
| External `/btw` seeding | `.work/pi087/btw-verify/verify-btw.mjs` drives the patched `btw.ts` verbatim (host import redirected to an instrumented real-host shim): first request carries main context once, later requests do not repeat the seed, a restored side thread carries prior Q&A once, reset drops it, shutdown leaves main history untouched. The baseline extension fails the same harness on 0.87.0. |
| Responses preparation and replay | Built-provider tests cover legacy Context and transcript input, system sections, full transcripts versus slices, additive tools versus deletion/redeclaration, namespace/grammar mapping, tool-search pairing, model folding, prewarm and compaction replay. One preparation path owns tool placement. |
| Transcript and rendering | Lifecycle tests preserve stable identity across streaming/finalization, duplicate-end suppression, separate thinking-run clocks, exploration boundaries, selection provenance and teardown. Preview fixtures no longer encode private message-key strings. |
| Goal state | Host-entry tests cover replacement during a turn, cached-token accounting, elapsed time, pause/edit/resume, branch restore, completed-goal accounting, continuation filtering and shutdown/reload timer release. State extraction preserved the original seven behavior cases; lifecycle coverage now also guards retained contexts. |
| ANSI rendering | Extended RGB/indexed/colon colors consume their parameters before ordered DIM/background decisions. Two regression tests fail against the previous implementation and pass now; non-SGR control sequences remain unchanged. |
| Root configuration / todo | Existing config precedence, command/tool outcomes and schema tests remain green. |
| Vendor configuration / settings | 1,782 differential configuration fixtures and 385 setting updates match the baseline, including labels, choices, action markers and immutable updates to the latest draft. Maintained tests cover aliases, defaults, optional values, dependent flags and all 15 simple toggles. |
| Notebook state | Generated capture code tests cover values/functions/metadata, partial writes, close/commit, caps and scope-specific skips/errors. Hashed payloads and checkpoint layout mismatches are tested. |
| Shell truncation | 270 comparisons against the saved implementation match across empty/long input, narrow/wide output and row budgets. |

The former real-host smoke covered tool titles, write states, mouse folding, skills, chrome and real `apply_patch` execution. Its unique entry and patch contracts now live in `test/contract/host-entry.test.mjs`; related rules and host component checks remain in focused tests. An earlier isolated `DefaultResourceLoader` probe with installed Pi 0.87.1 confirmed that the shared patch snapshot reaches the Codex renderer. The current strict PTY journey checks live input, mouse, rendering and mock-provider callbacks; no paid provider request was made.

## Limits

- The strict PTY run passed locally with tmux and Node 24.15.0; the new CI job on Node 22 has not run yet. E1–E6 use separate Pi boots and workspaces; each can run alone with `-- --journey=E3` (replace E3 as needed).
- The CI lane runs `npm run verify` on Pi 0.87.0. `@earendil-works/pi-ai` 0.85.x does not export `normalizeContext`, so the 0.85.1 lane failed at import; 0.86.1 exports it and the suite loads with the 0.87-only context-edit cases skipped by their capability gate (verified against a 0.86.1 host). Runtime code still avoids 0.87-only static imports.
- Codex provider tests use offline payload capture. No real paid Codex request or server-produced encrypted compaction checkpoint was used; compaction/checkpoint fixtures are offline substitutes.
- The `/btw` patch is delivered under `.work/pi087/btw-fix` (baseline `mitsuhiko/agent-stuff` `122e299`); it was not applied to the installed extension cache, not published and not referenced from `settings.json`.
- Notebook capture tests exercise generated code in Node, not the native Deno runtime. Native payload binaries were not rebuilt or changed.
- No real OS clipboard or image-terminal validation. Mouse selection and Ctrl+C reach a local sink installed through the real host clipboard method; the selected text is compared byte for byte there.
- No user configuration, credentials, default model, plugin sources or installed Pi were changed. Running Pi processes need a restart to load rebuilt vendor code.

Vendor baseline, retained payload exclusions and patch maintenance are documented in [UPSTREAM.md](vendor/pi-codex-conversion/UPSTREAM.md) and [PATCHES.md](vendor/pi-codex-conversion/PATCHES.md).
